<?php

namespace CoreBundle\Repository;
use CoreBundle\Entity\Alert;
use CoreBundle\Entity\Garden;

/**
 * HistoryNotificationRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class HistoryNotificationRepository extends \Doctrine\ORM\EntityRepository
{
    public function countNotificationLast24Hours(Garden $garden, Alert $alert)
    {
        $qb = $this->createQueryBuilder('h')
                ->select('COUNT(h.id)')
                ->where('h.garden = :garden')
                ->andWhere('h.alert = :alert')
                ->setParameters([
                    'garden' => $garden,
                    'alert' => $alert,
                ]);

        return $qb->getQuery()->getSingleScalarResult();
    }
}
